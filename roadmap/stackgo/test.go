/*
// Tests unitaires

package greetings

import "testing"

func TestHello(t *testing.T) {
	name := "Alice"
	want := "Bonjour, Alice!"
	if got := Hello(name); got != want {
		t.Errorf("Hello(%q) = %q, want %q", name, got, want)
	}
}

*/